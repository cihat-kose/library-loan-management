services:
  db:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: library_loans
      MYSQL_USER: library_app
      # Defaults make the local demo start without a .env file. Override them
      # for any non-demo deployment.
      MYSQL_PASSWORD: ${DB_PASSWORD:-library-app-local}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-library-root-local}
    ports:
      - "127.0.0.1:3306:3306"
    volumes:
      - library_data:/var/lib/mysql
      - ./sql/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
      - ./sql/seed.sql:/docker-entrypoint-initdb.d/02-seed.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost --silent"]
      interval: 5s
      timeout: 5s
      retries: 30
volumes:
  library_data:
